#pragma once

/* Prevent Opus/windows.h (the p-code wrapper) from shadowing the modern SDK
 * when original source directories are added to the compiler search path. */
#include "opus_windows_sdk.h"

