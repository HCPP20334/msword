/*
 * Compile-only template boundary for Opus/dlg/about.des.  The full template
 * must be regenerated by the ported historical dialog compiler before the
 * About dialog is runtime-active.
 */
DLG_CONST DLT dltAbout = { 0 };
